<div class="bg-light py-3">
    <div class="container">
        <div class="row">
            <div class="col-md-12 mb-0">
                <a href="/Home">Home</a>
                <span class="mx-2 mb-0">/</span>
                <a href="/@ViewBag.Title">@ViewBag.Title</a>
                @if (ViewBag.Title == "Detail") {
                    <span class="mx-2 mb-0">/</span>
                    
                }
            </div>
        </div>
    </div>
</div>
